## Highlights

- Carried forward every unpublished April 7 change from `release-2026-04-07-01` through `release-2026-04-07-06` into one final release candidate.
- Closed the remaining clean-room release gaps that still existed after the IM SDK portability fixes:
  - lazy vendored hub-installer resolution for desktop installer smoke
  - tracked Windows Tauri runtime config in the repository
  - portable tar test fixtures for Linux long-path release bundles
  - cross-platform release CLI path parsing that preserves explicit Windows paths on Linux hosts
- Revalidated the unified release architecture across desktop, server, docker, and kubernetes verification surfaces on both the Windows workspace and a Linux/WSL clean-room clone.
- Preserved the local relative-path shared SDK development model while keeping GitHub release automation pinned to Git-backed shared SDK sources.

## Attempt Outcome

- This is the next release candidate after the unpublished `release-2026-04-07-06` attempt.
- The candidate is ready for `main` push, tag creation, GitHub Release publication, and release workflow verification.
- If GitHub publication succeeds, this tag should become the first successful April 7 release and replace every earlier unpublished attempt.

## Verification Focus

- Confirm the carried-forward release candidate passes:
  - `pnpm lint`
  - Windows and WSL clean-room release-flow tests
  - `pnpm build`
  - `pnpm docs:build`
  - `pnpm check:server`
  - `pnpm check:desktop`
- Confirm `node scripts/release/render-release-notes.mjs --release-tag release-2026-04-07-07` renders one merged release body that includes `release-2026-04-07-01` through `release-2026-04-07-06`.
- Confirm GitHub Actions completes the release workflow for the new tag and that a GitHub Release object exists for `release-2026-04-07-07`.
